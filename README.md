# AI Video Pipeline

This project generates professional short video content (Shorts/Reels) automatically using AI. It automates everything from script writing to video rendering.

## 🚀 Features
- **AI Scripting**: Generates engaging, high-retention scripts using Groq (Llama 3).
- **Natural Voice**: Uses Edge TTS for realistic, free voiceovers.
- **Dynamic Visuals**: Fetches portrait-optimized visuals from Pexels API.
- **Smart Captions**: Automatically generates and burns subtitles into the video.
- **SEO Metadata**: Generates catchy titles, tags, and descriptions.
- **Custom Thumbnails**: Creates high-impact thumbnails programmatically.

## 🛠️ Tools Used
- **Python**: Core logic and orchestration.
- **Groq API**: Scripting and SEO metadata.
- **Edge TTS**: Text-to-speech engine.
- **Pexels API**: Stock video/image sourcing.
- **MoviePy**: Video editing and compositing.
- **Pillow**: Thumbnail generation.

## ⚙️ Installation & Setup

1. **Clone the repository**:
   ```bash
   git clone <your-repo-url>
   cd AI-Video-Pipeline
   ```

2. **Install dependencies**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Configure API Keys**:
   Create a `.env` file in the root directory and add your keys:
   ```env
   GROQ_API_KEY=your_groq_key_here
   PEXELS_API_KEY=your_pexels_key_here
   ```

4. **FFmpeg**: Ensure `ffmpeg` is installed on your system and added to PATH.

## 🎥 Usage

Run the pipeline:
```bash
python main.py
```
Enter a topic when prompted (e.g., "The history of Space Travel"), and the script will:
1. Generate a script.
2. Produce audio and subtitles.
3. Fetch relevant visuals.
4. Render the final `final_video.mp4`.
5. Open the video automatically.

---
*Generated by AI Video Pipeline Automator*
